import { ReactElement } from "react";
import { OrganizationScreen } from "../../screens/Organization";

const Organization = (): ReactElement => <OrganizationScreen />;

export default Organization;
